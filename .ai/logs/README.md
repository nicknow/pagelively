# logs/

Agent session logs and traces.

- **Git-ignored.** Logs are ephemeral; keep them here, not in commits.
- Long-lived operational knowledge (e.g. how to diagnose a recurring issue) is promoted to
  `docs/operations/` (the runbook) when it becomes durable.
