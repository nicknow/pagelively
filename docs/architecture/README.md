# Architecture (`docs/architecture/`)

How the pieces fit together: module boundaries and contracts, the data model (D1 schema,
`rev`), the caching model, error handling, and the concrete test strategy — with every
significant decision recorded as an ADR in `docs/adr/`.

**Status: not yet written.** This section is produced during the architecture phase (Phase 2)
and filled in as slices land. Until then, the authoritative description of the intended design
is `docs/product-spec.md`.
