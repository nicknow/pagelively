# API (`docs/api/`)

The request/response contracts for the public routes and the admin API.

**Status: not yet written.** Produced as the corresponding slices land (see
`docs/development/roadmap.md`). The intended routes are specified in `docs/product-spec.md`
§5 (URL & routing scheme) and §10 (admin UI & upload flows).
